export const getAccountState = (store: any) => store.accountState;

export const getContractState = (store: any) => store.contractState;

export const getLoanState = (store: any) => store.loanState;

export const getNftState = (store: any) => store.nftState;

export const getPawnState = (store: any) => store.pawnState;
